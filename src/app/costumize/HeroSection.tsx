import React from "react";
import Image from "next/image";
import styles from "./HeroSection.module.css"


const HeroSection = () => { 
    return (
        <section className={ styles.HeroSection} style={{ minHeight: '1700px' }}>
            
                    </section>

    );

}

export default HeroSection;
